/* Generated by openApiCatalogGenerate. Do not edit. */
import * as v from "valibot"
import { type Result } from "#result"
import type { OutscraperClient } from "../outscraperClientCreate.js"
import { outscraperRequest } from "../outscraperRequest.js"
import { outscraperResultErrorCreate } from "../outscraperResultErrorCreate.js"
import {
  type WhitepagesAddressesGetInput,
  whitepagesAddressesGetInputSchema,
} from "./whitepagesAddressesGetInputSchema.js"
import {
  type WhitepagesAddressesGetResponse,
  whitepagesAddressesGetResponseSchema,
} from "./whitepagesAddressesGetResponseSchema.js"

export async function whitepagesAddressesGet(
  client: OutscraperClient,
  input: WhitepagesAddressesGetInput,
): Promise<Result<WhitepagesAddressesGetResponse>> {
  const op = "whitepagesAddressesGet"
  const parsed = v.safeParse(whitepagesAddressesGetInputSchema, input)
  if (!parsed.success) {
    return outscraperResultErrorCreate(op, `Invalid whitepagesAddressesGet input: ${v.summarize(parsed.issues)}`)
  }

  const value = parsed.output
  return outscraperRequest(client, {
    op,
    path: "/whitepages-addresses",
    method: "GET",
    pathParams: value.path as Record<string, string | number | boolean>,
    params: value.query as Record<
      string,
      string | number | boolean | readonly (string | number | boolean)[] | null | undefined
    >,
    headers: value.headers as Record<
      string,
      string | number | boolean | readonly (string | number | boolean)[] | undefined
    >,

    schema: whitepagesAddressesGetResponseSchema,
  })
}
